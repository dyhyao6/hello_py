
根据 camera_info.json 文件
{
    "category": "224",
    "cameraId": "551932053507",
    "name": "1424(新)",
    "online": true
}
描述的是摄像头的基本信息
现在需要根据cameraId 去数据库查询一些字段：
1、根据 cameraId 去表algorithm_param_grid 中查询 point，index，gis字段 limit 1
2、根据 cameraId 去表 algorithm_param_post_safeguard 查询 type 、roi、roi_of_detection、 aircraft_model、 area_title、 name ，这里面是一个 cameraId 可能对应多条数据 ， 查询的数据是数组类型 字段名是 post_safeguard  
3、根据 cameraId 去表 algorithm_param_post_safety_scenarios 查询 type 、roi、roi_of_detection、 aircraft_model、 area_title、 name ，这里面是一个 cameraId 可能对应多条数据 ， 查询的数据是数组类型 字段名是 safety_scenarios


postgresql数据库
postgresql地址：	172.16.11.14:5432/edi_data_lianchuang
user:user_pCB6S3	
password:password_haW2jY

表：CREATE TABLE public.algorithm_param_grid (
	id int4 DEFAULT nextval('algorithm_param_grid_t_id_seq'::regclass) NOT NULL,
	camera_name varchar NULL,
	camera_id varchar NULL,
	point varchar NULL,
	"index" varchar NULL,
	gis varchar NULL,
	"version" int4 DEFAULT 1 NULL,
	created_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	updated_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT algorithm_param_grid_t_pkey PRIMARY KEY (id)
);

CREATE TABLE public.algorithm_param_post_safeguard (
	id int4 DEFAULT nextval('algorithm_param_post_safeguard_t_id_seq'::regclass) NOT NULL,
	camera_id varchar(255) NULL,
	"index" varchar NULL,
	"type" varchar NULL,
	roi varchar NULL,
	roi_of_detection varchar NULL,
	aircraft_model varchar NULL,
	area_title varchar NULL,
	"name" varchar NULL,
	"position" varchar NULL,
	camera_name varchar NULL,
	"version" int4 DEFAULT 1 NULL,
	created_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	updated_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT algorithm_param_post_safeguard_t_pkey PRIMARY KEY (id)
);

CREATE TABLE public.algorithm_param_post_safety_scenarios (
	id int4 DEFAULT nextval('algorithm_param_post_safety_scenarios_t_id_seq'::regclass) NOT NULL,
	camera_id varchar(255) NULL,
	"index" varchar NULL,
	"type" varchar NULL,
	roi varchar NULL,
	roi_of_detection varchar NULL,
	aircraft_model varchar NULL,
	area_title varchar NULL,
	"name" varchar NULL,
	"position" varchar NULL,
	camera_name varchar NULL,
	camera_direction varchar NULL,
	"version" int4 DEFAULT 1 NULL,
	created_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	updated_at timestamptz DEFAULT CURRENT_TIMESTAMP NULL,
	CONSTRAINT algorithm_param_post_safety_scenarios_t_pkey PRIMARY KEY (id)
);


最终产出的数据是：
{
    "cameraID": "551932053507",
    "name": "1424(新)",
    "isActive": true,  # 是否激活 固定为 true
	"areaCode": "224",  # 字段 category
	"point": [],  # 字段 point
	"index": [],  # 字段 index
	"geopoint": [],  # 字段 gis
	"postSafeguard": [],  # 字段 post_safeguard
	"safetyScenarios": [],  # 字段 safety_scenarios
}
的数组 并写入到新的json 文件 camera_data.json
